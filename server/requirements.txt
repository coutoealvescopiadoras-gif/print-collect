fastapi==0.115.6
uvicorn[standard]==0.34.0
gunicorn==23.0.0
sqlalchemy==2.0.36
psycopg[binary]==3.2.3
psycopg2-binary>=2.9.10
pydantic==2.10.3
pydantic-settings==2.7.0
python-multipart==0.0.20
bcrypt>=4.1.3
python-jose==3.3.0
ecdsa==0.19.2
fastapi-mail==1.4.1
python-dotenv==1.0.1
httpx==0.28.1
mangum==0.17.0
pg8000==1.31.3
# =================================================================
# SNMP BACKEND PARA IMPRESSORAS MANUAIS (Julio 06/09)
# -----------------------------------------------------------------
# O CODIGO no routes.py JA ESTA PRONTO e usa try/except TOTALMENTE SEGURO
# (se as libs abaixo NAO estiverem instaladas ele SKIPA silenciosamente
#  — NUNCA QUEBRA a resposta do agente nem da API!).
#
# O deploy inicial do Render falhou porque algumas destas bibliotecas
# precisam de compilação C. Para manter tudo SEGURO e ESTAVEL, DEIXAMOS
# COMENTADAS por PADRÃO. Para ATIVAR depois, basta:
#   1) Descomentar as 3 linhas abaixo
#   2) Commit & push (Render deploya)
#   3) Se falhar de novo, voltamos a comentar e pronto.
# =================================================================
# pysnmp-lextudio==6.1.7
# pyasn1==0.6.1
# pysmi-lextudio==1.5.10
