[tool.vercel]
# Entrypoint antigo (funcionava perfeitamente antes da Vercel CLI 58):
# wrapper Mangum em api/collect_api.py que ja faz sys.path.insert() corretamente.
entrypoint = "api.collect_api:handler"
